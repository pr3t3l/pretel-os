# Registro de cambios — auditoría Fase 0/1 → Fase 2 (2026-06-25)

Ancla de la auditoría: cada cambio de doctrina/build hecho en la simulación de Papandi. Cada auditor revisa SU archivo contra esta lista y reporta lo que todavía refleja el modelo viejo o contradice un cambio.

| # | Cambio | Por qué | Dónde debe aterrizar |
|---|---|---|---|
| **C1** | **Valor por FUNCIONALIDAD, no por comparación.** Se retira el value-stack de Hormozi: valores percibidos por pieza (`perceived_value_usd`), `comparable`/`value_rationale`, y el candado de ratio (valor ≥ N× precio). Cada pieza se justifica por lo que HACE / le permite al cliente y el freno que quita. (decision `a2ef7e37`) | Comparar pone el producto en la sombra de otros (suena caro/barato) e introduce competidores; viola el glass-box. | M1 §5 (instrumento, output `offer_spec`, gates, decision log), corpus (divergencia Hormozi), M2 (contenido vende por funcionalidad), build: `offer.ts`, `stack-proposal.ts`, `statement-proposal.ts`, `paquete-thread`, `guiones`, `threads/paquete` |
| **C2** | **Prueba de que vale = movimiento del eje débil** (`proposed_rescore`, p.ej. likelihood 3→6), NO un ratio de valor. | El "no-brainer" deja de ser matemático-comparativo. | M1 §5, build (stack-proposal, paquete-thread), M2 (la fuerza se mide por el eje) |
| **C3** | **Único candado económico = MARGEN, por forma de entrega** (digital 70% / servicio 50% / físico 30% / híbrido 40%). El LLM clasifica `delivery_format`; el código mapea el umbral (`MARGIN_TARGETS`). Un 70% fijo rompía la generalización a físico/servicio. | Salud del negocio, no comparación; generaliza a cualquier tipo de negocio. | M1 §5 margin gate, build (`offer.ts` `marginThresholdFor`, `stack-proposal` clasifica formato, `paquete-thread` selector) |
| **C4** | **Atacar el eje débil por MECANISMO + reversión de riesgo, NUNCA por prueba social.** Producto pre-lanzamiento = sin clientes → prohibido casos/testimonios/simulaciones/"a otros les funcionó". Mecanismo = "por qué te funciona a TI", anclado en los DIFERENCIADORES firmados. Reversión de riesgo accionable (prueba gratis, acompañamiento). Creencia por experiencia propia. Gobierna AMBOS generadores: el `weak_plan_note` de 1.1 y el `offer-stack-builder` de 1.2. (decision `11b919ab`) | Un escéptico no cree un testimonio ajeno; inventarlo es claim fabricada. | M1 §5 "cómo se ataca el eje débil", build (`value-equation-suggest`, `stack-proposal`), M2 (énfasis del contenido: mecanismo + experiencia, jamás "a otros les funcionó") |
| **C5** | **HÍBRIDO: la OFERTA es del BUYER PERSONA (una sola, compartida por todos sus avatares); la ESTRATEGIA/CONTENIDO es por avatar.** Diferenciador reencuadrado: "N estrategias de contenido/distribución por avatar, sobre UNA oferta del comprador". JAMÁS "ofertas separadas" por avatar. | Coherencia con la arquitectura; no romper el contrato con Fase 2. | `Overall_WF` (diferenciador), M1 (oferta=persona), M2 (contenido por avatar sobre una oferta), tabla de capas en `CLAUDE.md`, build (`statement-proposal`: nunca "separate offers") |
| **C6** | **Buyer Persona (0.3 `personas.persona`) ≠ ICP (0.2.5) ≠ Avatar (0.3 avatars[]).** La oferta y la ecuación de valor anclan en `personas.persona`, no en el ICP ni en un avatar. | Precisión; merge rompe B2B y pierde precisión B2C. | M0 (la distinción de capas), M1, tabla de capas `CLAUDE.md` |
| **C7** | **Economía de producto = capa Foundation (sub-paso 1.0).** `cost_to_serve_usd` + `base_price_usd` a nivel proyecto, se definen una vez y se reúsan entre avatares; **fuente única** = las cajas de pre-decisión; el paquete las ESPEJA (no copia que se desincroniza). (decision `897e55a3`) | Embebido por-avatar se re-preguntaba y divergía. | M1 (1.0 product_economics, 1.4 sella el precio), build (`product-economics` schema, `paquete-thread` sync) |
| **C8** | **Precio = estudio REAL** (competidores firmados + web), sobre el buyer persona completo (no un canal). El `math_gate` de 0.2 era viabilidad, no precio. | "El precio sugerido fue inventado" — cazada del operador. | M1 1.2/1.4, build (`pricing-research`) |
| **C9** | **Costo de servir = motor dinámico** por tipo de negocio (checklist de costos olvidados, `payment_mode` direct/marketplace, amortización de molde, comisiones de marketplace). NO sale del ledger `project_llm_calls` del wizard. | El ledger es costo de DESARROLLO, no de servir; rompía la generalización (productos no-IA). | build (`cost-estimate`), nota M1 |
| **C10** | **DataForSEO ACTIVO** (volumen real de Google, best-effort con fallback a estimación del LLM). El claim "datos reales de búsqueda" es honesto cuando corre el camino real. | El operador confirmó que funciona y está activo. (La memoria `papandi-keyword-data` que decía "son estimaciones" quedó desactualizada.) | M0 (0.2 demand quantification), M2 (contenido usa keyword data real), build (`market-research` route, `dataforseo.ts`) |
| **C11** | **Página de venta (1.4 statement) = por funcionalidad, UNA línea de precio, UN solo idioma (el del mercado, del launch focus), traduciendo las semillas en español; sin "separate offers".** | La página alimenta TODO el contenido de Fase 2. | build (`statement-proposal`), M1 1.4, M2 |
| **C12** | **Garantía y urgencia 100% honestas:** garantía solo si se honra (checkbox `honor_confirmed`); urgencia solo si es real (`genuine_reason`); cuentas regresivas inventadas prohibidas; displacement obligatorio. Garantía de ENTREGABLE, no de resultado de ventas. | Honestidad arquitectural irreductible. | M1 1.3, M2, build (`offer-steps` GarantiaStep) |
| **C13** | **UI: flujo principal magro, la EDUCACIÓN al panel derecho "De dónde sale"; cada cosa UNA vez** (sin narrar-y-luego-repetir). Rediseño de 1.2. | Eliminar redundancia; el panel enseña, la columna actúa. | build (`paquete-thread`, `HelpPanel` STEP_HELP). Principio que puede extenderse a otros pasos/fases |
| **C14** | **Estrategia de idioma:** se construye en español (comodidad del operador), el mercado primario es US/inglés; la salida al mercado va en el idioma del locale; se traduce al final. | Operador trabaja en español; el cliente final ve su idioma. | M2 (idioma del contenido), build (`statement-proposal`, language packs) |
| **C15** | **Frontera híbrida DENTRO de Fase 2: reparto 2.1 + voz 2.0 = COMPARTIDOS del buyer persona; piezas 2.2–2.5 = por avatar.** El `awareness_distribution` se mide UNA vez (0.2, compartido); el `content_mix` (2.1) se deriva de esa foto y se comparte. **Prohibido** sesgar el mix por-avatar desde una etiqueta (`journey_stage`) — error de Schwartz (un avatar no es una sola etapa; va de novato a experto). La diferenciación por avatar vive en las PIEZAS: canales (2.2) anclados en `avatars[].where_we_meet`, pilares (2.3) desde `forces_of_progress` DECLARADAS (no re-inferidas), ganchos (2.5) en su vocabulario. Per-avatar awareness se habilita **solo con medición** (keyword research por-avatar = próximo paso). Almacenamiento: `project_phase_artifacts.avatar_key` ('' compartido / `key` por avatar). | El operador cazó que el reparto salía idéntico entre avatares (foto compartida) Y las piezas no se diferenciaban (datos del avatar ignorados). Honestidad de medición > fabricar 4 fotos. Schwartz: el awareness es por-segmento PERO solo si se mide. | M2 (`spec_Phase_2_Contenido.md` §0, §4, §2.2, §2.3 + header), `Overall_WF` (nota scope of the loop), build: `canon.ts` (`isP2StepPerAvatar` excluye 2.1), `route.ts` (inyecta `where_we_meet` + `forces_of_progress` en 2.2–2.5), `page.tsx` (selector), migración `avatar_key`. **Decisión a registrar en pretel-os: D-008.** |

## Próximo paso documentado (post-C15)
- **Keyword research POR AVATAR** (enriquecimiento, costo aparte): correr DataForSEO con los términos de búsqueda específicos de CADA avatar (derivables de `where_we_meet` + `essence` + `forces_of_progress`), para (a) alimentar las piezas 2.2–2.5 con keywords propias del segmento, y (b) habilitar — con MEDICIÓN honesta — un `awareness_distribution` por-avatar que permita subir el reparto 2.1 a per-avatar. Se ejecuta **apenas cierre esta etapa** (mandato del operador 2026-06-26). La infra (`avatar_key`) ya lo soporta: bastaría volver `isP2StepPerAvatar` true para 2.1 cuando exista la medición.

## Notas de aplicación
- M1 (`spec_Phase_1_Oferta.md`) ya fue parcheado a v1.8 pero **tiene residuos**: el output `offer_spec.json` aún muestra `perceived_value_usd`/`value_rationale`; el `price_rationale` aún "cita stack ratio"; el gate-check aún menciona ratio. Hay que limpiarlos o marcarlos legacy.
- M2 (`spec_Phase_2_Contenido.md`) **parcheado a v1.2 (C15, 2026-06-26)**: frontera 2.1 compartido + piezas por avatar. Pendiente futuro: revisar residuos del modelo `unified_C_*`/`language_packs` si cambia la orquestación.
- El **build de Fase 2** (`lib/wizard/phase2`, `components/phase2`, `app/api/phase2`) se alinea con C15 en esta sesión: `avatar_key` (migración aplicada), `isP2StepPerAvatar`, inyección de `where_we_meet` + `forces_of_progress` en 2.2–2.5.
