"""pretel-os Telegram bot — Module 5.

Mobile-first capture and review surface. Runs as a separate systemd
unit; bot imports MCP tool functions directly per
`specs/telegram_bot/plan.md` Q2 (no HTTP / MCP-protocol round-trip).
"""
